{
  "name": "teamhyeokintro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:pages": "node scripts/generate-app-pages.js",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "prebuild": "npm run generate:pages && npm run generate:sitemap",
    "build": "npm run generate:pages && npm run generate:sitemap"
  }
}
